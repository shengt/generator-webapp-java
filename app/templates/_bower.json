{
  "name": "<%= _.slugify(appname) %>",
  "version": "0.0.0",
  "dependencies": {<% if (bootstrap && stylus) { %>
    "bootstrap-stylus": "~3.0.0",<% } %><% if (includeModernizr) { %>
    "modernizr": "~2.6.2",<% } %>
    "jquery": "~1.10.2"<% if (oldIE) { %>
    "requirejs": "~2.1.9"<% } %>
  },
  "devDependencies": {}
}
